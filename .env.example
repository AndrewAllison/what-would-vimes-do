# OpenAI API key (required)
OPENAI_API_KEY=your_api_key_here

# Port for the server (optional, default: 4111)
PORT=4111

# Node environment (optional, default: production for Docker deployment)
NODE_ENV=production
